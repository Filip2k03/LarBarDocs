import { AppRegistry } from 'react-native';
import { PassengerApp } from './src/PassengerApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => PassengerApp);
