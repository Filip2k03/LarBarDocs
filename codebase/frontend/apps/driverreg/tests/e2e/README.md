# Native E2E

The release pipeline must exercise Android and iOS fresh install, permission allow/deny, camera capture/retake, interrupted upload, process death/restore, offline recovery, large fonts, screen readers, notification routing, and activation deep links on physical-device or simulator farms. Provider test tenants must return real sandbox results; the client has no fake success mode.
