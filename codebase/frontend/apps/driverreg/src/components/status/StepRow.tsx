import React from 'react'; import {StyleSheet,Text,View} from 'react-native'; import {AppIcon,AppIconName} from '../../design-system/icons/AppIcon'; import {colors} from '../../design-system/colors';
export function StepRow({label,icon,state}:{label:string;icon:AppIconName;state:'complete'|'pending'|'correction'}){const color=state==='complete'?colors.success:state==='correction'?colors.warning:'#8A8782';return <View style={s.row}><View style={[s.icon,{backgroundColor:`${color}18`}]}><AppIcon name={icon} color={color}/></View><Text style={s.label}>{label}</Text><Text style={[s.state,{color}]}>{state==='complete'?'Complete':state==='correction'?'Action required':'Not complete'}</Text></View>}
const s=StyleSheet.create({row:{minHeight:62,flexDirection:'row',alignItems:'center',borderBottomWidth:1,borderBottomColor:colors.border,gap:12},icon:{width:38,height:38,borderRadius:12,alignItems:'center',justifyContent:'center'},label:{flex:1,fontSize:14,fontWeight:'700',color:colors.text},state:{fontSize:11,fontWeight:'700'}});
